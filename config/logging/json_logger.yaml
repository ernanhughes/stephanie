# config/logging/json_logger.yaml
logger:
  type: JSONLogger
  use_event_table: true
  log_path: "./logs/"
  report_path: "./reports/pipeline_events.jsonl"
  #  log_file: ${hydra:job.name}/logs/pipeline_log.jsonl if you need to set the log file name
  
logging:
  logger:
    type: JSONLogger
    use_event_table: true
    log_path: "./logs/"
    report_path: "./reports/pipeline_events.jsonl"