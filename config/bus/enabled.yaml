enabled: true
backend: nats            # "nats" | "inproc"
servers: "nats://localhost:4222"
stream: stephanie
required: false          # if true (or strict true), fail instead of fallback
strict: false            # legacy alias for required
connect_timeout_s: 1.5   # fail fast if NATS isn’t up
fallback: inproc         # "inproc" | "none"
